 # 26
 
 class Solution {
public:
    int removeDuplicates(vector<int>& nums) {
        int i=0;
        int prev = -101;
        int j=0;
        while(j<nums.size())
        {
            if(nums[j]==prev)
            {   j++;
                continue;
                
            }
            else {
                prev = nums[j];
                nums[i]=prev;
                i++;
                j++;
            }
        }
        
        return i;
    }
};
